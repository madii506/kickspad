# KICKS
Launch a coin on any Kick streamer. Fees send Kicks on stream.
Single-file site + readers (`api/kick.js`, `api/handle.js`, `api/tweet.js`, `api/avatar.js`, `api/coin.js`). No database, nothing writes. A human presses send — Kick has no gifting API.
